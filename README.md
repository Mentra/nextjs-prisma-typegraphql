## `nextjs-prisma-typegraphql`

A boilerplate with Next.js, Prisma, and TypeGraphQL, all running together.

## Necessary Software
1. Docker
2. Node.js

## Setup

1. `npm run db:create`
2. `npm run db:start`
3. `npm run migrate`
